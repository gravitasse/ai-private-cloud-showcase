# Temporary static inventory for dev/aws
# Replace these with real IPs once Terraform is applied,
# or wire this up to generated inventory from terraform output.

[k8s_masters]
master1 ansible_host=1.2.3.4 ansible_user=ubuntu

[k8s_workers]
worker1 ansible_host=5.6.7.8 ansible_user=ubuntu
